import { App } from '../../src/shell/app';

describe('the app', () => {
    it('says hello', () => {
        //expect(new App({} as any, {} as any, {} as any).message).toBe('Hello World!');
    });
});
